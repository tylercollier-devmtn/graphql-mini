type Person {
  id: Int
  name: String
  gender: String
  homeworld: HomeWorld
}

type HomeWorld {
  name: String
  climate: String
  population: Int
}

type Query {
  people: [Person]
  person(id: Int): Person
}